## 任务

为 `GET /api/generate` 增加 `mode` 参数，支持 `classic` 与 `human` 两种生成策略，同时保持 `length`、`domainIndex` 兼容。

## 计划

1. 在服务端新增真人风格邮箱本地部分生成器。
2. 在 `/api/generate` 中接入 `mode` 参数，并加入唯一性重试。
3. 更新 API 文档，补充新参数与示例。
