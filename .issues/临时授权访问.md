## 任务

- 管理员为当前邮箱生成临时授权码
- 支持通过 `/info/:code` 免登录只读查看指定邮箱邮件列表
- 临时访问模式仅保留自动刷新与手动刷新

## 实施计划

1. 扩展 `mailboxes` 表，保存临时授权码并补迁移逻辑。
2. 新增管理员生成临时授权码接口，并在认证层支持基于授权码的只读身份。
3. 改造首页前端，增加授权按钮与结果展示，并为 `/info/:code` 启用只读模式。
